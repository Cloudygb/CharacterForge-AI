# Desktop App Icons

Tauri expects production icon assets in this directory. The current Step 7.1 desktop shell uses the default generated app metadata and keeps icon replacement as a later branding task.

Before publishing an installer, replace this note with generated `.ico`, `.icns`, and PNG icon assets using the Tauri icon workflow.
